# SDD-051 --- CSV and Excel Exports

Export the transactions that match the list filters (period, type,
status, category, tag) as CSV (pt-BR) or XLSX, from Web and native
(FR-088, DR-097). Amounts are exact; each row carries the transaction
id.
