# SDD-053 --- Portable Backup

Download a versioned JSON backup of the user's data:
- every accessible space with its records (including deleted
  transactions and the audit-relevant state);
- the user's global goals.

The backup is distinct from reports (FR-089, DR-097). Restore is out of
scope.
