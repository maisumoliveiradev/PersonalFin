# SDD-049 --- v0.8.0 Release Validation

Validate SDD-045 to SDD-048 from a fresh clone and a clean database:
- run all automated checks and browser journeys;
- check native startup;
- record the report;
- release `v0.8.0` (merge to `main`, tag).
