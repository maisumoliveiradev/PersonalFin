# SDD-039 --- v0.6.0 Release Validation

Validate SDD-034 to SDD-038 from a fresh clone and clean database, run
all automated checks and browser journeys (including multi-user
journeys), check native startup, record the report, and release
`v0.6.0` (merge to `main`, tag).
