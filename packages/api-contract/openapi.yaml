openapi: 3.1.0
info:
  title: PersonalFin Application API
  version: 0.8.0
  description: >
    Application API contract. Authentication endpoints (sign-up, sign-in,
    sign-out, session) are served by Better Auth under /api/auth and are
    consumed through the Better Auth client; they are not part of this
    contract (ADR-0007).


    Clients send their version in the X-Client-Version header
    (MAJOR.MINOR.PATCH). When the operator configures a minimum client
    version, every operation except /health answers
    426 CLIENT_UPGRADE_REQUIRED to older or unversioned clients
    (ADR-0016).
security:
  - sessionCookie: []
paths:
  /health:
    get:
      operationId: getHealth
      summary: Report API liveness
      security: []
      responses:
        '200':
          description: The API process is running and able to serve requests.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthStatus'
  /me:
    get:
      operationId: getCurrentUser
      summary: Return the authenticated user
      responses:
        '200':
          description: The user bound to the current session.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUser'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
  /financial-spaces:
    get:
      operationId: listFinancialSpaces
      summary: List the Financial Spaces the user can access
      responses:
        '200':
          description: Accessible Financial Spaces, oldest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinancialSpaceList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
    post:
      operationId: createFinancialSpace
      summary: Create a Financial Space owned by the user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateFinancialSpaceRequest'
      responses:
        '201':
          description: The created Financial Space. The creator is its Owner.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinancialSpace'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
  /financial-spaces/{spaceId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getFinancialSpace
      summary: Return one accessible Financial Space
      responses:
        '200':
          description: The Financial Space.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FinancialSpace'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/categories:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listCategories
      summary: List the categories of an accessible Financial Space
      description: >
        Top-level categories ordered by position, each with its
        subcategories, including archived ones (flagged). Every new space
        receives the default catalog once.
      responses:
        '200':
          description: The category tree of the space.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CategoryList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createCategory
      summary: Create a category, or a subcategory when parentCategoryId is set
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCategoryRequest'
      responses:
        '201':
          description: The created category.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '409':
          $ref: '#/components/responses/CategoryConflict'
        '422':
          description: The parent is not an active top-level category of the same kind (code PARENT_CATEGORY_NOT_AVAILABLE).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/categories/{categoryId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - name: categoryId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      operationId: updateCategory
      summary: Rename, archive, or unarchive a category (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCategoryRequest'
      responses:
        '200':
          description: The category after the change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CategoryNotFound'
        '409':
          $ref: '#/components/responses/CategoryConflict'
    delete:
      operationId: deleteCategory
      summary: Permanently delete a never-used category without subcategories
      parameters:
        - name: version
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '204':
          description: Deleted.
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CategoryNotFound'
        '409':
          $ref: '#/components/responses/CategoryConflict'
  /financial-spaces/{spaceId}/transactions:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listTransactions
      summary: List the most recent transactions of an accessible Financial Space
      description: >
        Ordered by financial date (newest first), then by creation time
        (newest first).
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 100
        - name: state
          in: query
          required: false
          description: >
            active (default) lists non-deleted transactions by financial date;
            deleted lists the trash, most recently deleted first.
          schema:
            type: string
            enum:
              - active
              - deleted
            default: active
        - name: month
          in: query
          required: false
          description: Calendar month of the financial date (YYYY-MM).
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        - name: type
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TransactionType'
        - name: status
          in: query
          required: false
          schema:
            $ref: '#/components/schemas/TransactionStatus'
        - name: categoryId
          in: query
          required: false
          description: Matches the category or the subcategory.
          schema:
            type: string
            format: uuid
        - name: tagId
          in: query
          required: false
          description: Transactions carrying this tag.
          schema:
            type: string
            format: uuid
        - name: q
          in: query
          required: false
          description: Case- and accent-insensitive search in the description.
          schema:
            type: string
            minLength: 1
            maxLength: 100
        - name: cursor
          in: query
          required: false
          description: Opaque cursor from a previous page's nextCursor.
          schema:
            type: string
      responses:
        '200':
          description: The most recent transactions of the space.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionList'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createTransaction
      summary: Register a manual income or expense in an accessible Financial Space
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTransactionRequest'
      responses:
        '200':
          description: >
            Replay of a request whose client id was already created by the
            same user in this space; the existing transaction is returned
            and nothing is created (ADR-0016).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '201':
          description: The created transaction, recorded in the space currency (BRL).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '409':
          description: >
            The client id is already used by another space or another user
            (code TRANSACTION_ID_CONFLICT).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          $ref: '#/components/responses/TransactionRejected'
  /financial-spaces/{spaceId}/transactions/{transactionId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/TransactionId'
    get:
      operationId: getTransaction
      summary: Return one transaction of an accessible Financial Space
      responses:
        '200':
          description: The transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/TransactionNotFound'
    patch:
      operationId: updateTransaction
      summary: Edit a transaction (partial update with optimistic concurrency)
      description: >
        Only the fields present are changed. The final state must satisfy
        the same rules as creation. Every effective change is recorded in
        the audit log; a request that changes nothing is accepted without
        a new version.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTransactionRequest'
      responses:
        '200':
          description: The transaction after the edit.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/TransactionNotFound'
        '409':
          $ref: '#/components/responses/StateConflict'
        '422':
          $ref: '#/components/responses/TransactionRejected'
    delete:
      operationId: deleteTransaction
      summary: Soft delete a transaction (restorable, audited)
      parameters:
        - name: version
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
        - name: syncResolution
          in: query
          required: false
          description: Sync context of a deletion that resolves a conflict (with syncBaseVersion).
          schema:
            $ref: '#/components/schemas/SyncResolution'
        - name: syncBaseVersion
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
      responses:
        '200':
          description: The deleted transaction (deletedAt set).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/TransactionNotFound'
        '409':
          $ref: '#/components/responses/StateConflict'
  /financial-spaces/{spaceId}/transactions/{transactionId}/restore:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/TransactionId'
    post:
      operationId: restoreTransaction
      summary: Restore a soft-deleted transaction (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VersionRequest'
      responses:
        '200':
          description: The restored transaction.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/TransactionNotFound'
        '409':
          $ref: '#/components/responses/StateConflict'
  /financial-spaces/{spaceId}/balance-snapshots:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listBalanceSnapshots
      summary: List observed balance snapshots, most recent first
      description: >
        Ordered by observed date, then recording instant (newest first).
        current is the first item: the latest observed consolidated balance.
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 100
      responses:
        '200':
          description: Snapshot history of the space.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalanceSnapshotList'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: recordBalanceSnapshot
      summary: Record an observed consolidated balance (append-only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordBalanceSnapshotRequest'
      responses:
        '201':
          description: The recorded snapshot. Earlier snapshots are never changed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalanceSnapshot'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/balance-reminder:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getBalanceReminder
      summary: Return the user's balance reminder setting for the space
      description: Returns the default (every 7 days) with isDefault true when never saved.
      responses:
        '200':
          description: The effective setting.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalanceReminder'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    put:
      operationId: setBalanceReminder
      summary: Save the user's balance reminder setting for the space
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BalanceReminderSetting'
      responses:
        '200':
          description: The saved setting.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalanceReminder'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/dashboard:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getMonthlyDashboard
      summary: Built-in metrics of one month (see docs/product/METRICS.md)
      parameters:
        - name: month
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
      responses:
        '200':
          description: Metric values M-001 to M-007 for the month.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonthlyDashboard'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/recurrences:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listRecurrences
      summary: List recurring series of the space
      responses:
        '200':
          description: Series, oldest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecurrenceSeriesList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createRecurrence
      summary: Create a recurring income or expense series
      description: >
        Creates the series and its Pending occurrences through the end of
        the month 12 months after the current month. Each occurrence is an
        ordinary transaction linked by recurrenceSeriesId.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRecurrenceRequest'
      responses:
        '201':
          description: The series and how many occurrences were created.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecurrenceSeriesCreated'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '422':
          $ref: '#/components/responses/CategoryNotAvailable'
  /financial-spaces/{spaceId}/recurrences/{seriesId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/SeriesId'
    patch:
      operationId: updateRecurrenceFrom
      summary: Change the series defaults for this and following occurrences
      description: >
        Updates the series and every Pending, non-deleted occurrence on or
        after fromOccurrenceDate that was not individually modified. Paid,
        past-edited, and deleted occurrences are never rewritten. Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateRecurrenceRequest'
      responses:
        '200':
          description: The series after the change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecurrenceSeriesChanged'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/RecurrenceNotFound'
        '409':
          $ref: '#/components/responses/StateConflict'
        '422':
          $ref: '#/components/responses/CategoryNotAvailable'
  /financial-spaces/{spaceId}/recurrences/{seriesId}/end:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/SeriesId'
    post:
      operationId: endRecurrence
      summary: End a series on a date
      description: >
        Sets the end date and moves to the trash every Pending, unmodified
        occurrence scheduled after it. Everything else is kept. Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - version
                - endDate
              properties:
                version:
                  type: integer
                  minimum: 1
                endDate:
                  $ref: '#/components/schemas/FinancialDate'
      responses:
        '200':
          description: The ended series.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RecurrenceSeriesChanged'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/RecurrenceNotFound'
        '409':
          $ref: '#/components/responses/StateConflict'
  /financial-spaces/{spaceId}/recurrences/materialize:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    post:
      operationId: materializeRecurrences
      summary: Create missing occurrences of every series through a month
      description: Idempotent. Never goes beyond 60 months after the current month.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - throughMonth
              properties:
                throughMonth:
                  type: string
                  pattern: '^\d{4}-(0[1-9]|1[0-2])$'
      responses:
        '200':
          description: Number of occurrences created by this call.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required:
                  - occurrencesCreated
                properties:
                  occurrencesCreated:
                    type: integer
                    minimum: 0
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/projection:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getProjectionSeries
      summary: Projected month-end balances (M-008) for consecutive months
      parameters:
        - name: fromMonth
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        - name: months
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 24
            default: 6
      responses:
        '200':
          description: One projected month-end balance per month (null without an observed balance).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectionSeries'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/commitments:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getCommitments
      summary: Pending income and expenses that are overdue or coming up
      description: >
        overdue = Pending, non-deleted transactions dated before from;
        upcoming = those dated from `from` through from + days - 1. Items
        are ordered by financial date; each section returns at most 200
        items plus exact totals.
      parameters:
        - name: from
          in: query
          required: true
          description: The user's current calendar date.
          schema:
            type: string
            format: date
        - name: days
          in: query
          required: false
          schema:
            type: integer
            minimum: 7
            maximum: 365
            default: 30
      responses:
        '200':
          description: Overdue and upcoming commitments.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Commitments'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/cards:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listCards
      summary: List the credit cards of the space
      responses:
        '200':
          description: Active cards first, then archived, by name.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createCard
      summary: Register a credit card with its initial limit
      description: Creates the card and the first entry of its limit history. Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCardRequest'
      responses:
        '201':
          description: The created card.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Card'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '409':
          $ref: '#/components/responses/CardConflict'
  /financial-spaces/{spaceId}/cards/{cardId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
    get:
      operationId: getCard
      summary: Get a card with its limit history
      responses:
        '200':
          description: The card.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Card'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
    patch:
      operationId: updateCard
      summary: Rename, change closing/due days, or archive a card
      description: Version-checked and audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCardRequest'
      responses:
        '200':
          description: The card after the change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Card'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
        '409':
          $ref: '#/components/responses/CardConflict'
  /financial-spaces/{spaceId}/cards/{cardId}/limit-changes:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
    post:
      operationId: recordCardLimit
      summary: Record a new card limit from an effective date
      description: >
        Appends to the limit history; earlier values are never changed
        (FR-051). The current limit on a date is the latest value effective
        on or before it (ties: latest recorded).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RecordCardLimitRequest'
      responses:
        '201':
          description: The card with its updated limit history.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Card'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
  /financial-spaces/{spaceId}/cards/{cardId}/invoices/{month}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
      - $ref: '#/components/parameters/InvoiceMonth'
    get:
      operationId: getCardInvoice
      summary: Get the invoice of a card for a reference month with its purchases
      responses:
        '200':
          description: The invoice.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardInvoice'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
  /financial-spaces/{spaceId}/cards/{cardId}/invoices/{month}/dates:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
      - $ref: '#/components/parameters/InvoiceMonth'
    put:
      operationId: setCardInvoiceDates
      summary: Override the closing and due dates of one invoice
      description: >
        Changes only this invoice (DR-038); the card defaults and the
        purchases already assigned to it are unchanged. Version-checked and
        audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InvoiceDatesRequest'
      responses:
        '200':
          description: The invoice after the change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardInvoice'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
        '409':
          $ref: '#/components/responses/StateConflict'
  /financial-spaces/{spaceId}/installment-purchases/{purchaseId}/cancel:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - name: purchaseId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: cancelInstallments
      summary: Cancel the installments of a purchase after an invoice month
      description: >
        Moves to the trash the installments whose invoice month is after
        afterMonth. Earlier installments are kept (DR-043). Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - afterMonth
              properties:
                afterMonth:
                  type: string
                  pattern: '^\d{4}-(0[1-9]|1[0-2])$'
      responses:
        '200':
          description: How many installments were cancelled.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required:
                  - cancelled
                properties:
                  cancelled:
                    type: integer
                    minimum: 0
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: The purchase does not exist in this space (code INSTALLMENT_PURCHASE_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/cards/{cardId}/invoices/{month}/payments:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
      - $ref: '#/components/parameters/InvoiceMonth'
    post:
      operationId: payCardInvoice
      summary: Record a full or partial invoice payment
      description: >
        Settles already-recorded card expenses; it never creates an expense
        (DR-036). The amount cannot exceed the open amount (DR-039). Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - amountMinor
                - paidOn
              properties:
                amountMinor:
                  $ref: '#/components/schemas/AmountMinor'
                paidOn:
                  $ref: '#/components/schemas/FinancialDate'
      responses:
        '201':
          description: The invoice after the payment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardInvoice'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
        '422':
          description: The invoice has no open amount this large (code PAYMENT_EXCEEDS_OUTSTANDING).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/cards/{cardId}/invoices/{month}/payments/{paymentId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
      - $ref: '#/components/parameters/InvoiceMonth'
      - name: paymentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: removeCardInvoicePayment
      summary: Remove an invoice payment recorded by mistake
      description: Soft-deletes the payment; audited.
      responses:
        '200':
          description: The invoice after the removal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardInvoice'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: CARD_NOT_FOUND or INVOICE_PAYMENT_NOT_FOUND.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/card-limits:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getCardLimits
      summary: Known used and available limit of every card on a date
      description: >
        usedMinor is the unpaid part of all invoices of the card (every
        non-deleted purchase, including future installments, minus
        payments). availableMinor = current limit on the date - usedMinor;
        it may be negative and is null when no limit is effective yet.
      parameters:
        - name: 'on'
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        '200':
          description: One entry per card.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardLimits'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/cards/{cardId}/invoices:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/CardId'
    get:
      operationId: listCardInvoices
      summary: Invoice totals of a card for consecutive months
      parameters:
        - name: fromMonth
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        - name: months
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 24
            default: 6
      responses:
        '200':
          description: One entry per month, including months without purchases.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CardInvoiceSummaryList'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/CardNotFound'
  /financial-spaces/{spaceId}/tags:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listTags
      summary: List the tags of the space
      responses:
        '200':
          description: Active tags first, then archived, by name.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TagList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createTag
      summary: Create a tag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - name
              properties:
                name:
                  type: string
                  minLength: 1
                  maxLength: 40
      responses:
        '201':
          description: The created tag.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '409':
          $ref: '#/components/responses/TagConflict'
  /financial-spaces/{spaceId}/tags/{tagId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - name: tagId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      operationId: updateTag
      summary: Rename, archive, or unarchive a tag
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - version
              properties:
                version:
                  type: integer
                  minimum: 1
                name:
                  type: string
                  minLength: 1
                  maxLength: 40
                archived:
                  type: boolean
      responses:
        '200':
          description: The tag after the change.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/TagNotFound'
        '409':
          $ref: '#/components/responses/TagConflict'
    delete:
      operationId: deleteTag
      summary: Permanently delete a never-used tag
      parameters:
        - name: version
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '204':
          description: Deleted.
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/TagNotFound'
        '409':
          $ref: '#/components/responses/TagConflict'
  /financial-spaces/{spaceId}/analytics/evolution:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getEvolution
      summary: Month-by-month realized metrics and observed balance (M-009)
      parameters:
        - name: fromMonth
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        - name: months
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 24
            default: 12
      responses:
        '200':
          description: One entry per month, oldest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Evolution'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/analytics/comparison:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getComparison
      summary: Compare a month with the previous month and the same month a year earlier (M-010)
      parameters:
        - name: month
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
      responses:
        '200':
          description: The comparison.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comparison'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/analytics/breakdown:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getBreakdown
      summary: Realized expenses of a month range by category and tag (M-011, M-012)
      parameters:
        - name: fromMonth
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        - name: months
          in: query
          required: false
          schema:
            type: integer
            enum:
              - 1
              - 3
              - 6
              - 12
            default: 1
      responses:
        '200':
          description: >
            Categories add up exactly to totalMinor. A transaction counts in
            full under each of its tags, so tag amounts may add up to more
            than totalMinor (DR-013). Items include those with an amount only
            in the previous range of equal length.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Breakdown'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/dashboard-preferences:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getDashboardPreferences
      summary: The current user's dashboard preferences for the space
      description: Defaults to the advanced profile without overrides (isDefault true).
      responses:
        '200':
          description: Preferences and the resolved section visibility.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardPreferences'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    put:
      operationId: saveDashboardPreferences
      summary: Save the experience profile and section overrides
      description: >
        Overrides equal to the profile default are dropped. Preferences
        only change visibility for this user; values are never affected.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - profile
                - overrides
              properties:
                profile:
                  $ref: '#/components/schemas/ExperienceProfile'
                overrides:
                  $ref: '#/components/schemas/SectionOverrides'
      responses:
        '200':
          description: The saved preferences.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DashboardPreferences'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/invitations:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listInvitations
      summary: Invitations of the space, newest first (requires manage_members)
      responses:
        '200':
          description: Invitations with their status.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createInvitation
      summary: Invite a person by email with a preset or a permission set
      description: >
        Returns a single-use token once; only its hash is stored. The link
        expires after 7 days. Email delivery is not implemented yet
        (TD-011): the inviter shares the link. Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                preset:
                  type: string
                  enum:
                    - viewer
                    - contributor
                    - administrator
                permissions:
                  type: array
                  items:
                    $ref: '#/components/schemas/SpacePermission'
      responses:
        '201':
          description: The invitation and its token (shown only now).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatedInvitation'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '409':
          description: ALREADY_MEMBER or INVITATION_PENDING.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/invitations/{invitationId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - name: invitationId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: cancelInvitation
      summary: Cancel a pending invitation
      responses:
        '204':
          description: Cancelled.
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: FINANCIAL_SPACE_NOT_FOUND or INVITATION_NOT_FOUND.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '410':
          description: INVITATION_NOT_AVAILABLE (already accepted or cancelled).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invitations/{token}:
    parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
    get:
      operationId: previewInvitation
      summary: What an invitation link grants (any signed-in user holding the token)
      responses:
        '200':
          description: The invitation preview.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvitationPreview'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '404':
          description: INVITATION_NOT_FOUND.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invitations/{token}/accept:
    parameters:
      - name: token
        in: path
        required: true
        schema:
          type: string
    post:
      operationId: acceptInvitation
      summary: Accept an invitation with the signed-in account of the invited email
      responses:
        '200':
          description: The joined space.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required:
                  - spaceId
                properties:
                  spaceId:
                    type: string
                    format: uuid
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          description: INVITATION_EMAIL_MISMATCH.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: INVITATION_NOT_FOUND.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: ALREADY_MEMBER.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '410':
          description: INVITATION_NOT_AVAILABLE (expired, cancelled, or used).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/members:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listMembers
      summary: The Owner and active members of the space
      responses:
        '200':
          description: Owner first, then members by joining date.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemberList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/members/{userId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - name: userId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      operationId: changeMemberPermissions
      summary: Replace a member's permissions (view is always kept)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - version
                - permissions
              properties:
                version:
                  type: integer
                  minimum: 1
                permissions:
                  type: array
                  items:
                    $ref: '#/components/schemas/SpacePermission'
      responses:
        '204':
          description: Changed; audited.
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: FINANCIAL_SPACE_NOT_FOUND or MEMBER_NOT_FOUND (including the Owner).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      operationId: removeMember
      summary: Remove a member; records they created stay in the space
      parameters:
        - name: version
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        '204':
          description: Removed; audited.
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: FINANCIAL_SPACE_NOT_FOUND or MEMBER_NOT_FOUND.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: VERSION_CONFLICT or OWNER_CANNOT_LEAVE (the Owner cannot be removed).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/leave:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    post:
      operationId: leaveSpace
      summary: Leave a shared space (members only)
      responses:
        '204':
          description: Left; audited.
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '409':
          description: OWNER_CANNOT_LEAVE (transfer ownership first, DR-009).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/ownership-transfer:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    post:
      operationId: transferOwnership
      summary: Transfer ownership to an active member (Owner only)
      description: >
        The new Owner's membership ends and they hold every permission as
        Owner; the previous Owner becomes an Administrator member. Exactly
        one Owner before and after (DR-006). Audited.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - newOwnerUserId
              properties:
                newOwnerUserId:
                  type: string
                  format: uuid
      responses:
        '204':
          description: Transferred.
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: FINANCIAL_SPACE_NOT_FOUND or MEMBER_NOT_FOUND.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/audit-events:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listAuditEvents
      summary: Audit history of the space, newest first (requires view_audit)
      parameters:
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        '200':
          description: A page of audit events.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditHistory'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/debts:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listDebts
      summary: List the debts of the space with their summaries
      responses:
        '200':
          description: Debts in creation order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createDebt
      summary: Register a debt or loan (audited)
      description: >
        Debts are tracked by value and progress without interest (DR-045, DR-092); they do not create transactions.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateDebtRequest'
      responses:
        '201':
          description: The created debt.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '422':
          description: >
            INVALID_DEBT_PLAN (installment larger than the original amount).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/debts/{debtId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/DebtId'
    get:
      operationId: getDebt
      summary: Get a debt with its payments
      responses:
        '200':
          description: The debt.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the debt does not exist (FINANCIAL_SPACE_NOT_FOUND, DEBT_NOT_FOUND, DEBT_PAYMENT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: updateDebt
      summary: Edit or archive a debt (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateDebtRequest'
      responses:
        '200':
          description: The updated debt.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the debt does not exist (FINANCIAL_SPACE_NOT_FOUND, DEBT_NOT_FOUND, DEBT_PAYMENT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: >
            INVALID_DEBT_PLAN (installment above the original amount, or fewer installments than already paid).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/debts/{debtId}/payments:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/DebtId'
    post:
      operationId: recordDebtPayment
      summary: Record a debt payment (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DebtPaymentRequest'
      responses:
        '201':
          description: The debt with the new payment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the debt does not exist (FINANCIAL_SPACE_NOT_FOUND, DEBT_NOT_FOUND, DEBT_PAYMENT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: >
            DEBT_OVERPAYMENT (larger than the outstanding balance).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/debts/{debtId}/payments/{paymentId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/DebtId'
      - name: paymentId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    delete:
      operationId: removeDebtPayment
      summary: Remove a debt payment (soft delete, audited)
      responses:
        '200':
          description: The debt without the payment.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the debt does not exist (FINANCIAL_SPACE_NOT_FOUND, DEBT_NOT_FOUND, DEBT_PAYMENT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/debts/{debtId}/simulations:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/DebtId'
    post:
      operationId: simulateDebtPrepayment
      summary: Simulate a prepayment without changing any data
      description: >
        Stateless: nothing is stored (DR-046, DR-047).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrepaymentSimulationRequest'
      responses:
        '200':
          description: The current and simulated states and the resulting plan (DR-093).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrepaymentSimulation'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the debt does not exist (FINANCIAL_SPACE_NOT_FOUND, DEBT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: >
            DEBT_OVERPAYMENT (larger than the outstanding balance).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/debts/{debtId}/prepayments:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/DebtId'
    post:
      operationId: confirmDebtPrepayment
      summary: Confirm a prepayment and apply the simulated plan (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmPrepaymentRequest'
      responses:
        '201':
          description: The debt with the prepayment and the new plan.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DebtDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the debt does not exist (FINANCIAL_SPACE_NOT_FOUND, DEBT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '422':
          description: >
            DEBT_OVERPAYMENT (larger than the outstanding balance).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /goals:
    get:
      operationId: listGoals
      summary: List the callers global goals
      responses:
        '200':
          description: Goals in creation order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
    post:
      operationId: createGoal
      summary: Create a global goal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGoalRequest'
      responses:
        '201':
          description: The created goal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
  /goals/{goalId}:
    parameters:
      - $ref: '#/components/parameters/GoalId'
    get:
      operationId: getGoal
      summary: Get a goal with its progress history
      responses:
        '200':
          description: The goal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '404':
          description: >
            The goal does not exist in this scope (GOAL_NOT_FOUND); global goals of other users are never visible.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: updateGoal
      summary: Edit or archive a goal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGoalRequest'
      responses:
        '200':
          description: The updated goal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '404':
          description: >
            The goal does not exist in this scope (GOAL_NOT_FOUND); global goals of other users are never visible.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /goals/{goalId}/progress:
    parameters:
      - $ref: '#/components/parameters/GoalId'
    post:
      operationId: recordGoalProgress
      summary: Set the accumulated amount (kept in the progress history)
      description: >
        Goals are manually maintained and never change projections (DR-049, DR-050).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoalProgressRequest'
      responses:
        '200':
          description: The goal with the new progress.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '404':
          description: >
            The goal does not exist in this scope (GOAL_NOT_FOUND); global goals of other users are never visible.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/goals:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listSpaceGoals
      summary: List the space goals
      responses:
        '200':
          description: Goals in creation order.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createSpaceGoal
      summary: Create a space goal (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateGoalRequest'
      responses:
        '201':
          description: The created goal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/goals/{goalId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/GoalId'
    get:
      operationId: getSpaceGoal
      summary: Get a goal with its progress history
      responses:
        '200':
          description: The goal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The goal does not exist in this scope (GOAL_NOT_FOUND), or the space is not accessible (FINANCIAL_SPACE_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    patch:
      operationId: updateSpaceGoal
      summary: Edit or archive a goal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateGoalRequest'
      responses:
        '200':
          description: The updated goal.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The goal does not exist in this scope (GOAL_NOT_FOUND), or the space is not accessible (FINANCIAL_SPACE_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/goals/{goalId}/progress:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/GoalId'
    post:
      operationId: recordSpaceGoalProgress
      summary: Set the accumulated amount (kept in the progress history)
      description: >
        Goals are manually maintained and never change projections (DR-049, DR-050).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoalProgressRequest'
      responses:
        '200':
          description: The goal with the new progress.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoalDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The goal does not exist in this scope (GOAL_NOT_FOUND), or the space is not accessible (FINANCIAL_SPACE_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/reminders:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listReminders
      summary: Current in-app reminders of the caller for a date
      description: >
        Personal: pending transactions, open card invoices, debt installments, and a negative projected month-end balance, filtered by the caller's reminder settings and dismissals.
      parameters:
        - name: today
          in: query
          required: true
          description: The caller's local calendar date.
          schema:
            type: string
            format: date
      responses:
        '200':
          description: Overdue reminders first, then by due date (DR-095).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReminderList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/reminders/dismissals:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    post:
      operationId: dismissReminder
      summary: Dismiss a reminder until its next stage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReminderDismissalRequest'
      responses:
        '204':
          description: Dismissed.
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/reminder-settings:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getReminderSettings
      summary: The caller's reminder settings for the space (defaults when never saved)
      responses:
        '200':
          description: The settings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReminderSettings'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    put:
      operationId: saveReminderSettings
      summary: Save the caller's reminder settings for the space
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReminderSettings'
      responses:
        '200':
          description: The saved settings (deduplicated and ordered).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReminderSettings'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
  /financial-spaces/{spaceId}/imports:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: listImports
      summary: List the space's imports (discarded drafts excluded)
      responses:
        '200':
          description: Newest first.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportList'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
    post:
      operationId: createImport
      summary: Read a CSV or Excel file into a draft import
      description: >
        Read step of Read → Validate → Preview → Resolve → Confirm → Import (DR-059). The original file is kept for traceability. At most 5 MB.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateImportRequest'
      responses:
        '201':
          description: The draft with its first rows for mapping.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '422':
          description: >
            IMPORT_FILE_UNREADABLE, IMPORT_FILE_EMPTY, or IMPORT_TOO_MANY_ROWS (more than 5000 rows).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    get:
      operationId: getImport
      summary: Get an import with its mapping and counts
      responses:
        '200':
          description: The import.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}/rows:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    get:
      operationId: listImportRows
      summary: Preview rows with their parsed values, errors, and duplicate candidates
      parameters:
        - name: filter
          in: query
          schema:
            type: string
            enum: [all, invalid, duplicates, importable]
            default: all
        - name: offset
          in: query
          schema:
            type: integer
            minimum: 0
            default: 0
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        '200':
          description: A page of rows.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportRowPage'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}/mapping:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    put:
      operationId: mapImport
      summary: Map columns and validate every row (resets duplicate decisions)
      description: >
        Date order, decimal separator, sign convention, and fallback categories are chosen explicitly; nothing ambiguous is guessed (DR-058). Suspected duplicates (same type, date, and amount as an existing transaction or an earlier row) need a decision (DR-060).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImportMappingRequest'
      responses:
        '200':
          description: The import with its counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT, IMPORT_NOT_DRAFT, IMPORT_NOT_MAPPED, IMPORT_DUPLICATES_UNDECIDED, IMPORT_NOTHING_TO_IMPORT, IMPORT_CATEGORY_CHANGED, or IMPORT_NOT_IMPORTED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}/decisions:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    put:
      operationId: decideImportDuplicates
      summary: Decide whether suspected duplicates are imported or skipped
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImportDecisionsRequest'
      responses:
        '200':
          description: The import with its counts.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT, IMPORT_NOT_DRAFT, IMPORT_NOT_MAPPED, IMPORT_DUPLICATES_UNDECIDED, IMPORT_NOTHING_TO_IMPORT, IMPORT_CATEGORY_CHANGED, or IMPORT_NOT_IMPORTED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}/confirm:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    post:
      operationId: confirmImport
      summary: Create the transactions of every importable row (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VersionRequest'
      responses:
        '200':
          description: The imported batch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT, IMPORT_NOT_DRAFT, IMPORT_NOT_MAPPED, IMPORT_DUPLICATES_UNDECIDED, IMPORT_NOTHING_TO_IMPORT, IMPORT_CATEGORY_CHANGED, or IMPORT_NOT_IMPORTED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}/undo:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    post:
      operationId: undoImport
      summary: Move every transaction of a confirmed import to the trash (audited)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VersionRequest'
      responses:
        '200':
          description: The undone batch.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT, IMPORT_NOT_DRAFT, IMPORT_NOT_MAPPED, IMPORT_DUPLICATES_UNDECIDED, IMPORT_NOTHING_TO_IMPORT, IMPORT_CATEGORY_CHANGED, or IMPORT_NOT_IMPORTED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/imports/{importId}/discard:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
      - $ref: '#/components/parameters/ImportId'
    post:
      operationId: discardImport
      summary: Discard a draft import
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VersionRequest'
      responses:
        '200':
          description: The discarded draft.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportDetail'
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          description: >
            The space or the import does not exist (FINANCIAL_SPACE_NOT_FOUND, IMPORT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: >
            VERSION_CONFLICT, IMPORT_NOT_DRAFT, IMPORT_NOT_MAPPED, IMPORT_DUPLICATES_UNDECIDED, IMPORT_NOTHING_TO_IMPORT, IMPORT_CATEGORY_CHANGED, or IMPORT_NOT_IMPORTED.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/exports/transactions:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: exportTransactions
      summary: Export the active transactions matching the filters (CSV or Excel)
      description: >
        A report, not a backup (DR-097).
      parameters:
        - name: format
          in: query
          required: true
          schema:
            type: string
            enum: [csv, xlsx]
        - name: month
          in: query
          description: Calendar month (YYYY-MM); not combined with from/to.
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
        - name: from
          in: query
          schema:
            type: string
            format: date
        - name: to
          in: query
          schema:
            type: string
            format: date
        - name: type
          in: query
          schema:
            $ref: '#/components/schemas/TransactionType'
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/TransactionStatus'
        - name: categoryId
          in: query
          schema:
            type: string
            format: uuid
        - name: tagId
          in: query
          schema:
            type: string
            format: uuid
        - name: q
          in: query
          schema:
            type: string
            minLength: 1
            maxLength: 100
      responses:
        '200':
          description: >
            The file as an attachment. CSV is UTF-8 with BOM, semicolon-separated,
            with decimal commas; XLSX has date and number cells.
          content:
            text/csv:
              schema:
                type: string
                format: binary
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
        '422':
          description: >
            EXPORT_TOO_LARGE (more than 50,000 transactions).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /financial-spaces/{spaceId}/reports/monthly:
    parameters:
      - $ref: '#/components/parameters/SpaceId'
    get:
      operationId: getMonthlyReport
      summary: Monthly PDF report (metrics, expenses by category, transactions)
      description: >
        Uses the canonical metric definitions (DR-066, DR-067). A report, not a backup (DR-097).
      parameters:
        - name: month
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}-(0[1-9]|1[0-2])$'
      responses:
        '200':
          description: The PDF as an attachment.
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          $ref: '#/components/responses/Unauthenticated'
        '426':
          $ref: '#/components/responses/ClientUpgradeRequired'
        '400':
          $ref: '#/components/responses/ValidationFailed'
        '403':
          $ref: '#/components/responses/PermissionDenied'
        '404':
          $ref: '#/components/responses/FinancialSpaceNotFound'
components:
  parameters:
    ImportId:
      name: importId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    GoalId:
      name: goalId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    DebtId:
      name: debtId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    InvoiceMonth:
      name: month
      in: path
      required: true
      schema:
        type: string
        pattern: '^\d{4}-(0[1-9]|1[0-2])$'
    CardId:
      name: cardId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SeriesId:
      name: seriesId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    TransactionId:
      name: transactionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    SpaceId:
      name: spaceId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: personalfin.session_token
      description: >
        Session cookie issued by Better Auth. Native clients send the same
        cookie through the Cookie header.
  responses:
    ClientUpgradeRequired:
      description: >
        The X-Client-Version header is missing, invalid, or lower than the
        configured minimum client version (code CLIENT_UPGRADE_REQUIRED).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TagNotFound:
      description: The tag does not exist in this space (code TAG_NOT_FOUND).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TagConflict:
      description: VERSION_CONFLICT, TAG_NAME_TAKEN, or TAG_IN_USE (archive instead).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TransactionRejected:
      description: >
        CATEGORY_NOT_AVAILABLE (category not usable for the type),
        TAG_NOT_AVAILABLE (tag missing or archived),
        CARD_NOT_AVAILABLE (missing or archived card), or
        INVALID_CARD_PURCHASE (income, status sent, invoice out of range, or
        invoice change on a non-card transaction).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    CardNotFound:
      description: The card does not exist in this space (code CARD_NOT_FOUND).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    CardConflict:
      description: VERSION_CONFLICT or CARD_NAME_TAKEN (another card of the space has the name).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    RecurrenceNotFound:
      description: The series does not exist in this space (code RECURRENCE_NOT_FOUND).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    CategoryNotFound:
      description: The category does not exist in this space (code CATEGORY_NOT_FOUND).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    CategoryConflict:
      description: >
        VERSION_CONFLICT, CATEGORY_NAME_TAKEN (sibling with the same name),
        CATEGORY_IN_USE (used by transactions, including deleted ones;
        archive instead), or CATEGORY_HAS_SUBCATEGORIES.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    CategoryNotAvailable:
      description: >
        The category is not a top-level category of this space with the
        same kind as the transaction type, or the subcategory does not
        belong to it (code CATEGORY_NOT_AVAILABLE).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    TransactionNotFound:
      description: The transaction does not exist in this space (code TRANSACTION_NOT_FOUND).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    StateConflict:
      description: >
        The request conflicts with the record's current state: the version
        is outdated (VERSION_CONFLICT), the transaction is deleted and cannot
        be edited (TRANSACTION_DELETED), or it is already deleted / not
        deleted (TRANSACTION_ALREADY_DELETED, TRANSACTION_NOT_DELETED).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    ValidationFailed:
      description: The request is invalid (code VALIDATION_FAILED).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    FinancialSpaceNotFound:
      description: >
        The Financial Space does not exist or is not accessible to the user
        (code FINANCIAL_SPACE_NOT_FOUND). Both cases return 404 so that
        inaccessible spaces are not revealed.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    PermissionDenied:
      description: >
        The user is a member of the space but lacks the permission for this
        action (code PERMISSION_DENIED).
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
    Unauthenticated:
      description: No valid session was provided.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
  schemas:
    ImportStatus:
      type: string
      enum: [draft, imported, undone, discarded]
    ImportMapping:
      type: object
      additionalProperties: false
      required: [hasHeader, columns, dateFormat, decimalSeparator, amountSign, fallbackCategoryIds, defaultStatus]
      properties:
        hasHeader:
          type: boolean
        columns:
          type: object
          additionalProperties: false
          description: Zero-based column indexes.
          required: [date, description, amount, type, category, subcategory, status]
          properties:
            date:
              type: integer
              minimum: 0
            description:
              type: integer
              minimum: 0
            amount:
              type: integer
              minimum: 0
            type:
              type: [integer, 'null']
              minimum: 0
            category:
              type: [integer, 'null']
              minimum: 0
            subcategory:
              type: [integer, 'null']
              minimum: 0
            status:
              type: [integer, 'null']
              minimum: 0
        dateFormat:
          type: string
          enum: [DMY, YMD]
          description: Order of day, month, and year in text dates; ISO dates are always accepted.
        decimalSeparator:
          type: string
          enum: [',', '.']
        amountSign:
          type: string
          enum: [type_column, negative_is_expense, all_expenses, all_income]
        fallbackCategoryIds:
          type: object
          additionalProperties: false
          required: [expense, income]
          properties:
            expense:
              type: [string, 'null']
              format: uuid
            income:
              type: [string, 'null']
              format: uuid
        defaultStatus:
          $ref: '#/components/schemas/TransactionStatus'
    ImportCounts:
      type: object
      additionalProperties: false
      required: [total, valid, invalid, duplicates, undecidedDuplicates, toImport]
      properties:
        total:
          type: integer
        valid:
          type: integer
        invalid:
          type: integer
          description: Includes the header row when there is one.
        duplicates:
          type: integer
        undecidedDuplicates:
          type: integer
        toImport:
          type: integer
    ImportSummary:
      type: object
      additionalProperties: false
      required: [id, fileName, format, status, rowCount, importedCount, createdAt, confirmedAt, undoneAt, version]
      properties:
        id:
          type: string
          format: uuid
        fileName:
          type: string
        format:
          type: string
          enum: [csv, xlsx]
        status:
          $ref: '#/components/schemas/ImportStatus'
        rowCount:
          type: integer
        importedCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        confirmedAt:
          type: [string, 'null']
          format: date-time
        undoneAt:
          type: [string, 'null']
          format: date-time
        version:
          type: integer
    ImportDetail:
      type: object
      additionalProperties: false
      required: [id, fileName, format, status, rowCount, importedCount, createdAt, confirmedAt, undoneAt, version, mapping, counts, firstRows]
      properties:
        id:
          type: string
          format: uuid
        fileName:
          type: string
        format:
          type: string
          enum: [csv, xlsx]
        status:
          $ref: '#/components/schemas/ImportStatus'
        rowCount:
          type: integer
        importedCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        confirmedAt:
          type: [string, 'null']
          format: date-time
        undoneAt:
          type: [string, 'null']
          format: date-time
        version:
          type: integer
        mapping:
          oneOf:
            - type: 'null'
            - $ref: '#/components/schemas/ImportMapping'
        counts:
          oneOf:
            - type: 'null'
            - $ref: '#/components/schemas/ImportCounts'
        firstRows:
          type: array
          items:
            type: array
            items:
              type: string
    ImportList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ImportSummary'
    ImportRow:
      type: object
      additionalProperties: false
      required: [rowNumber, cells, parsed, errors, duplicateOf, decision, transactionId]
      properties:
        rowNumber:
          type: integer
        cells:
          type: array
          items:
            type: string
        parsed:
          oneOf:
            - type: 'null'
            - type: object
              additionalProperties: false
              required: [type, status, description, amountMinor, financialDate, categoryId, subcategoryId, warnings]
              properties:
                type:
                  $ref: '#/components/schemas/TransactionType'
                status:
                  $ref: '#/components/schemas/TransactionStatus'
                description:
                  type: string
                amountMinor:
                  type: integer
                financialDate:
                  type: string
                  format: date
                categoryId:
                  type: string
                  format: uuid
                subcategoryId:
                  type: [string, 'null']
                  format: uuid
                warnings:
                  type: array
                  items:
                    type: string
                    enum: [category_fallback, subcategory_ignored]
        errors:
          type: array
          description: >
            header, date_invalid, description_empty, description_too_long, amount_empty,
            amount_invalid, amount_too_many_decimals, amount_zero, amount_too_large,
            amount_negative_ambiguous, type_unknown, status_unknown, category_unmatched.
          items:
            type: string
        duplicateOf:
          oneOf:
            - type: 'null'
            - type: object
              additionalProperties: false
              required: [kind, transactionId, description]
              properties:
                kind:
                  type: string
                  enum: [transaction]
                transactionId:
                  type: string
                  format: uuid
                description:
                  type: string
            - type: object
              additionalProperties: false
              required: [kind, rowNumber]
              properties:
                kind:
                  type: string
                  enum: [row]
                rowNumber:
                  type: integer
        decision:
          type: [string, 'null']
          enum: [import, skip, null]
        transactionId:
          type: [string, 'null']
          format: uuid
    ImportRowPage:
      type: object
      additionalProperties: false
      required: [items, hasMore]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/ImportRow'
        hasMore:
          type: boolean
    CreateImportRequest:
      type: object
      additionalProperties: false
      required: [fileName, format, contentBase64]
      properties:
        fileName:
          type: string
          minLength: 1
          maxLength: 200
        format:
          type: string
          enum: [csv, xlsx]
        contentBase64:
          type: string
          description: The file bytes in base64 (at most 5 MB decoded).
    ImportMappingRequest:
      type: object
      additionalProperties: false
      required: [version, mapping]
      properties:
        version:
          type: integer
          minimum: 1
        mapping:
          $ref: '#/components/schemas/ImportMapping'
    ImportDecisionsRequest:
      oneOf:
        - type: object
          additionalProperties: false
          required: [version, decisions]
          properties:
            version:
              type: integer
              minimum: 1
            decisions:
              type: array
              minItems: 1
              items:
                type: object
                additionalProperties: false
                required: [rowNumber, decision]
                properties:
                  rowNumber:
                    type: integer
                    minimum: 1
                  decision:
                    type: string
                    enum: [import, skip]
        - type: object
          additionalProperties: false
          required: [version, all]
          properties:
            version:
              type: integer
              minimum: 1
            all:
              type: string
              enum: [import, skip]
    ReminderStage:
      type: string
      enum: [overdue, before-0, before-1, before-3, before-7]
    ReminderSettings:
      type: object
      additionalProperties: false
      required: [offsets, kinds]
      properties:
        offsets:
          type: array
          maxItems: 4
          description: Days before the due date to remind (0 = on the day).
          items:
            type: integer
            enum: [0, 1, 3, 7]
        kinds:
          type: array
          items:
            type: string
            enum: [transactions, invoices, debts, projection]
    ReminderDismissalRequest:
      type: object
      additionalProperties: false
      required: [key, stage]
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 200
        stage:
          $ref: '#/components/schemas/ReminderStage'
    Reminder:
      type: object
      additionalProperties: false
      required: [key, kind, stage, dueDate, daysUntilDue, description, amountMinor, transactionType]
      properties:
        key:
          type: string
        kind:
          type: string
          enum: [transactions, invoices, debts, projection]
        stage:
          $ref: '#/components/schemas/ReminderStage'
        dueDate:
          type: string
          format: date
        daysUntilDue:
          type: integer
        description:
          type: string
          description: Transaction description, card name, debt name, or the month (YYYY-MM) for projection.
        amountMinor:
          type: integer
          description: Amount due; the projected balance (negative) for projection reminders.
        transactionType:
          oneOf:
            - type: 'null'
            - $ref: '#/components/schemas/TransactionType'
    ReminderList:
      type: object
      additionalProperties: false
      required: [today, items]
      properties:
        today:
          type: string
          format: date
        items:
          type: array
          items:
            $ref: '#/components/schemas/Reminder'
    GoalSummary:
      type: object
      additionalProperties: false
      required: [remainingMinor, progressTenths, reached]
      properties:
        remainingMinor:
          type: integer
        progressTenths:
          type: integer
          description: Accumulated share of the target in tenths of a percent, rounded down and capped at 1000.
        reached:
          type: boolean
    Goal:
      type: object
      additionalProperties: false
      required: [id, scope, name, targetAmountMinor, accumulatedMinor, currency, targetDate, archived, version, summary]
      properties:
        id:
          type: string
          format: uuid
        scope:
          type: string
          enum: [global, space]
        name:
          type: string
        targetAmountMinor:
          type: integer
        accumulatedMinor:
          type: integer
        currency:
          type: string
        targetDate:
          type: [string, 'null']
          format: date
        archived:
          type: boolean
        version:
          type: integer
        summary:
          $ref: '#/components/schemas/GoalSummary'
    GoalDetail:
      type: object
      additionalProperties: false
      required: [id, scope, name, targetAmountMinor, accumulatedMinor, currency, targetDate, archived, version, summary, progress]
      properties:
        id:
          type: string
          format: uuid
        scope:
          type: string
          enum: [global, space]
        name:
          type: string
        targetAmountMinor:
          type: integer
        accumulatedMinor:
          type: integer
        currency:
          type: string
        targetDate:
          type: [string, 'null']
          format: date
        archived:
          type: boolean
        version:
          type: integer
        summary:
          $ref: '#/components/schemas/GoalSummary'
        progress:
          type: array
          description: Accumulated-amount updates, newest first.
          items:
            type: object
            additionalProperties: false
            required: [accumulatedMinor, recordedAt]
            properties:
              accumulatedMinor:
                type: integer
              recordedAt:
                type: string
                format: date-time
    GoalList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Goal'
    CreateGoalRequest:
      type: object
      additionalProperties: false
      required: [name, targetAmountMinor]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 60
        targetAmountMinor:
          type: integer
          minimum: 1
        targetDate:
          type: [string, 'null']
          format: date
    UpdateGoalRequest:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 60
        targetAmountMinor:
          type: integer
          minimum: 1
        targetDate:
          type: [string, 'null']
          format: date
        archived:
          type: boolean
    GoalProgressRequest:
      type: object
      additionalProperties: false
      required: [version, accumulatedMinor]
      properties:
        version:
          type: integer
          minimum: 1
        accumulatedMinor:
          type: integer
          minimum: 0
    PrepaymentMode:
      type: string
      enum: [reduce_term, reduce_installment]
      description: reduce_term keeps the installment and lowers the count; reduce_installment keeps the count and lowers the installment.
    PrepaymentSimulationRequest:
      type: object
      additionalProperties: false
      required: [amountMinor, mode]
      properties:
        amountMinor:
          type: integer
          minimum: 1
        mode:
          $ref: '#/components/schemas/PrepaymentMode'
    ConfirmPrepaymentRequest:
      type: object
      additionalProperties: false
      required: [version, amountMinor, mode, paidOn]
      properties:
        version:
          type: integer
          minimum: 1
        amountMinor:
          type: integer
          minimum: 1
        mode:
          $ref: '#/components/schemas/PrepaymentMode'
        paidOn:
          type: string
          format: date
    PrepaymentSimulation:
      type: object
      additionalProperties: false
      required: [mode, amountMinor, plan, before, after]
      properties:
        mode:
          $ref: '#/components/schemas/PrepaymentMode'
        amountMinor:
          type: integer
        plan:
          type: object
          additionalProperties: false
          required: [installmentCount, installmentAmountMinor]
          properties:
            installmentCount:
              type: integer
            installmentAmountMinor:
              type: integer
        before:
          $ref: '#/components/schemas/DebtSummary'
        after:
          $ref: '#/components/schemas/DebtSummary'
    DebtSummary:
      type: object
      additionalProperties: false
      description: Derived from the plan and the active payments (DR-092).
      required: [paidMinor, outstandingMinor, paidInstallments, remainingInstallments, lastInstallmentMinor, nextDueDate, progressTenths, settled]
      properties:
        paidMinor:
          type: integer
        outstandingMinor:
          type: integer
        paidInstallments:
          type: integer
        remainingInstallments:
          type: integer
        lastInstallmentMinor:
          type: [integer, 'null']
        nextDueDate:
          type: [string, 'null']
          format: date
        progressTenths:
          type: integer
          description: Paid share of the original amount in tenths of a percent, rounded down.
        settled:
          type: boolean
    Debt:
      type: object
      additionalProperties: false
      required: [id, name, originalAmountMinor, currency, installmentCount, installmentAmountMinor, firstDueDate, archived, version, summary]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        originalAmountMinor:
          type: integer
        currency:
          type: string
        installmentCount:
          type: integer
        installmentAmountMinor:
          type: integer
        firstDueDate:
          type: string
          format: date
        archived:
          type: boolean
        version:
          type: integer
        summary:
          $ref: '#/components/schemas/DebtSummary'
    DebtPayment:
      type: object
      additionalProperties: false
      required: [id, kind, amountMinor, paidOn, recordedAt]
      properties:
        id:
          type: string
          format: uuid
        kind:
          type: string
          enum: [installment, prepayment]
        amountMinor:
          type: integer
        paidOn:
          type: string
          format: date
        recordedAt:
          type: string
          format: date-time
    DebtDetail:
      type: object
      additionalProperties: false
      required: [id, name, originalAmountMinor, currency, installmentCount, installmentAmountMinor, firstDueDate, archived, version, summary, payments]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        originalAmountMinor:
          type: integer
        currency:
          type: string
        installmentCount:
          type: integer
        installmentAmountMinor:
          type: integer
        firstDueDate:
          type: string
          format: date
        archived:
          type: boolean
        version:
          type: integer
        summary:
          $ref: '#/components/schemas/DebtSummary'
        payments:
          type: array
          items:
            $ref: '#/components/schemas/DebtPayment'
    DebtList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Debt'
    CreateDebtRequest:
      type: object
      additionalProperties: false
      required: [name, originalAmountMinor, installmentCount, firstDueDate]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 60
        originalAmountMinor:
          type: integer
          minimum: 1
        installmentCount:
          type: integer
          minimum: 1
          maximum: 600
        installmentAmountMinor:
          type: integer
          minimum: 1
          description: Defaults to the original amount divided by the installment count, rounded down.
        firstDueDate:
          type: string
          format: date
    UpdateDebtRequest:
      type: object
      additionalProperties: false
      required: [version]
      properties:
        version:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 60
        installmentCount:
          type: integer
          minimum: 1
          maximum: 600
        installmentAmountMinor:
          type: integer
          minimum: 1
        firstDueDate:
          type: string
          format: date
        archived:
          type: boolean
    DebtPaymentRequest:
      type: object
      additionalProperties: false
      required: [amountMinor, paidOn]
      properties:
        kind:
          type: string
          enum: [installment, prepayment]
          default: installment
        amountMinor:
          type: integer
          minimum: 1
        paidOn:
          type: string
          format: date
    CardDay:
      type: integer
      minimum: 1
      maximum: 31
      description: Day of the month; months without it use their last day.
    CardName:
      type: string
      minLength: 1
      maxLength: 60
    CardLimitChange:
      type: object
      additionalProperties: false
      required:
        - id
        - amountMinor
        - currency
        - effectiveFrom
        - recordedAt
      properties:
        id:
          type: string
          format: uuid
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        currency:
          type: string
          pattern: '^[A-Z]{3}$'
        effectiveFrom:
          $ref: '#/components/schemas/FinancialDate'
        recordedAt:
          type: string
          format: date-time
    Card:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - closingDay
        - dueDay
        - archived
        - version
        - limits
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        closingDay:
          $ref: '#/components/schemas/CardDay'
        dueDay:
          $ref: '#/components/schemas/CardDay'
        archived:
          type: boolean
        version:
          type: integer
          minimum: 1
        limits:
          type: array
          description: Limit history, latest effective date first (ties, latest recorded first).
          items:
            $ref: '#/components/schemas/CardLimitChange'
    CardList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Card'
    CreateCardRequest:
      type: object
      additionalProperties: false
      required:
        - name
        - closingDay
        - dueDay
        - limitMinor
        - limitEffectiveFrom
      properties:
        name:
          $ref: '#/components/schemas/CardName'
        closingDay:
          $ref: '#/components/schemas/CardDay'
        dueDay:
          $ref: '#/components/schemas/CardDay'
        limitMinor:
          $ref: '#/components/schemas/AmountMinor'
        limitEffectiveFrom:
          $ref: '#/components/schemas/FinancialDate'
    UpdateCardRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
        name:
          $ref: '#/components/schemas/CardName'
        closingDay:
          $ref: '#/components/schemas/CardDay'
        dueDay:
          $ref: '#/components/schemas/CardDay'
        archived:
          type: boolean
    RecordCardLimitRequest:
      type: object
      additionalProperties: false
      required:
        - amountMinor
        - effectiveFrom
      properties:
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        effectiveFrom:
          $ref: '#/components/schemas/FinancialDate'
    HealthStatus:
      type: object
      additionalProperties: false
      required:
        - status
      properties:
        status:
          type: string
          const: ok
    CurrentUser:
      type: object
      additionalProperties: false
      required:
        - id
        - email
        - name
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        name:
          type: string
    CreateFinancialSpaceRequest:
      type: object
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
          description: Trimmed, internal whitespace collapsed; 1 to 80 characters.
          minLength: 1
          maxLength: 80
    FinancialSpace:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - lifecycleState
        - role
        - permissions
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        lifecycleState:
          type: string
          enum:
            - active
        role:
          type: string
          description: The requesting user's role in the space.
          enum:
            - owner
            - member
        permissions:
          type: array
          description: >
            The requesting user's permissions (ADR-0015). The Owner has all
            of them.
          items:
            $ref: '#/components/schemas/SpacePermission'
        createdAt:
          type: string
          format: date-time
    SpacePermission:
      type: string
      enum:
        - view
        - record
        - plan
        - classify
        - manage_members
        - view_audit
    AuditHistory:
      type: object
      additionalProperties: false
      required:
        - items
        - nextCursor
      properties:
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - occurredAt
              - actorName
              - entityType
              - entityId
              - action
              - changes
              - context
            properties:
              context:
                description: >
                  How the change reached the server when it resolved an offline
                  synchronization conflict (ADR-0016); null otherwise.
                oneOf:
                  - type: 'null'
                  - type: object
                    additionalProperties: false
                    required:
                      - source
                      - resolution
                      - baseVersion
                    properties:
                      source:
                        type: string
                        enum:
                          - offline_sync
                      resolution:
                        $ref: '#/components/schemas/SyncResolution'
                      baseVersion:
                        type: integer
                        minimum: 1
              id:
                type: string
              occurredAt:
                type: string
                format: date-time
              actorName:
                type: string
              entityType:
                type: string
                enum:
                  - financial_transaction
                  - category
                  - recurrence_series
                  - card
                  - card_invoice
                  - card_invoice_payment
                  - tag
                  - financial_space
                  - financial_space_member
                  - space_invitation
                  - debt
                  - debt_payment
                  - goal
                  - import_batch
              entityId:
                type: string
              action:
                type: string
                enum:
                  - create
                  - update
                  - delete
                  - restore
              changes:
                type: object
                description: Field name to its value before and after.
                additionalProperties:
                  type: object
                  additionalProperties: false
                  required:
                    - before
                    - after
                  properties:
                    before:
                      type:
                        - string
                        - number
                        - boolean
                        - 'null'
                    after:
                      type:
                        - string
                        - number
                        - boolean
                        - 'null'
        nextCursor:
          type:
            - string
            - 'null'
    Member:
      type: object
      additionalProperties: false
      required:
        - userId
        - name
        - email
        - role
        - permissions
        - version
      properties:
        userId:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
        role:
          type: string
          enum:
            - owner
            - member
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/SpacePermission'
        version:
          type:
            - integer
            - 'null'
          description: Membership version; null for the Owner.
    MemberList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Member'
    InvitationStatus:
      type: string
      enum:
        - pending
        - accepted
        - cancelled
        - expired
    Invitation:
      type: object
      additionalProperties: false
      required:
        - id
        - email
        - permissions
        - status
        - createdAt
        - expiresAt
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/SpacePermission'
        status:
          $ref: '#/components/schemas/InvitationStatus'
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
    InvitationList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Invitation'
    CreatedInvitation:
      type: object
      additionalProperties: false
      required:
        - invitation
        - token
      properties:
        invitation:
          $ref: '#/components/schemas/Invitation'
        token:
          type: string
          description: Single-use secret for the invitation link; never returned again.
    InvitationPreview:
      type: object
      additionalProperties: false
      required:
        - spaceName
        - email
        - permissions
        - status
        - expiresAt
      properties:
        spaceName:
          type: string
        email:
          type: string
        permissions:
          type: array
          items:
            $ref: '#/components/schemas/SpacePermission'
        status:
          $ref: '#/components/schemas/InvitationStatus'
        expiresAt:
          type: string
          format: date-time
    FinancialSpaceList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/FinancialSpace'
    CategoryKind:
      type: string
      description: Transaction type a category applies to.
      enum:
        - expense
        - income
    Subcategory:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - archived
        - version
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        archived:
          type: boolean
        version:
          type: integer
          minimum: 1
    CategoryTreeItem:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - kind
        - archived
        - version
        - subcategories
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        kind:
          $ref: '#/components/schemas/CategoryKind'
        archived:
          type: boolean
          description: Archived categories are kept on existing transactions but not offered for new ones.
        version:
          type: integer
          minimum: 1
        subcategories:
          type: array
          items:
            $ref: '#/components/schemas/Subcategory'
    Category:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - kind
        - parentCategoryId
        - archived
        - version
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        kind:
          $ref: '#/components/schemas/CategoryKind'
        parentCategoryId:
          type:
            - string
            - 'null'
          format: uuid
        archived:
          type: boolean
        version:
          type: integer
          minimum: 1
    CreateCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 60
        kind:
          allOf:
            - $ref: '#/components/schemas/CategoryKind'
          description: Required for top-level categories; subcategories inherit the parent kind.
        parentCategoryId:
          type:
            - string
            - 'null'
          format: uuid
    UpdateCategoryRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
        name:
          type: string
          minLength: 1
          maxLength: 60
        archived:
          type: boolean
    CategoryList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/CategoryTreeItem'
    TransactionType:
      type: string
      enum:
        - expense
        - income
    TransactionStatus:
      type: string
      description: paid means Paid (expense) or Received (income).
      enum:
        - paid
        - pending
    AmountMinor:
      type: integer
      description: >
        Amount in the currency's minor units (for BRL, centavos). Always
        positive; the transaction type carries the direction.
      minimum: 1
      maximum: 99999999999
    FinancialDate:
      type: string
      format: date
      description: Calendar date without time or timezone (YYYY-MM-DD).
    CategoryReference:
      type: object
      additionalProperties: false
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
    CreateTransactionRequest:
      type: object
      additionalProperties: false
      required:
        - type
        - description
        - amountMinor
        - financialDate
        - categoryId
      properties:
        id:
          type: string
          format: uuid
          description: >
            Optional client-generated id (UUID) so a request can be
            retried safely, for example after an offline period. Not
            allowed with installments.
        type:
          $ref: '#/components/schemas/TransactionType'
        status:
          allOf:
            - $ref: '#/components/schemas/TransactionStatus'
          description: Defaults to paid.
        description:
          type: string
          description: Trimmed, internal whitespace collapsed; 1 to 140 characters.
          minLength: 1
          maxLength: 140
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        financialDate:
          $ref: '#/components/schemas/FinancialDate'
        categoryId:
          type: string
          format: uuid
          description: A top-level category of the space whose kind equals type.
        subcategoryId:
          type:
            - string
            - 'null'
          format: uuid
          description: Optional subcategory of categoryId.
        cardId:
          type: string
          format: uuid
          description: >
            Records a card purchase (expense only, active card of the space).
            Card purchases have no status of their own; do not send status.
        invoiceMonth:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])$'
          description: >
            Invoice (reference month) for a card purchase, from the month
            before to two months after financialDate. Defaults to the
            invoice whose closing date is after financialDate.
        tagIds:
          type: array
          maxItems: 10
          uniqueItems: true
          description: Active tags of the space; tags never change amounts (DR-013).
          items:
            type: string
            format: uuid
        installments:
          type: integer
          minimum: 2
          maximum: 48
          description: >
            Splits a card purchase into this many installments (amountMinor
            is the total; the remainder cents go to the first). Installment
            k is dated k - 1 months after financialDate and goes to the
            invoice k - 1 months after the first. The response is the first
            installment.
    Transaction:
      type: object
      additionalProperties: false
      required:
        - id
        - type
        - status
        - description
        - amountMinor
        - currency
        - financialDate
        - category
        - subcategory
        - createdAt
        - version
        - deletedAt
        - recurrenceSeriesId
        - occurrenceDate
        - cardPurchase
        - installment
        - tags
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: '#/components/schemas/TransactionType'
        status:
          $ref: '#/components/schemas/TransactionStatus'
        description:
          type: string
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        currency:
          type: string
          description: ISO 4217 code. Currently always BRL.
          pattern: '^[A-Z]{3}$'
        financialDate:
          $ref: '#/components/schemas/FinancialDate'
        category:
          $ref: '#/components/schemas/CategoryReference'
        subcategory:
          oneOf:
            - $ref: '#/components/schemas/CategoryReference'
            - type: 'null'
        createdAt:
          type: string
          format: date-time
        version:
          type: integer
          minimum: 1
          description: Incremented on every effective edit; send it back when editing.
        deletedAt:
          type:
            - string
            - 'null'
          format: date-time
          description: When the transaction was soft-deleted; null when active.
        recurrenceSeriesId:
          type:
            - string
            - 'null'
          format: uuid
          description: The series this transaction is an occurrence of; null for one-off entries.
        occurrenceDate:
          type:
            - string
            - 'null'
          format: date
          description: Scheduled (unadjusted) date of the occurrence; null for one-off entries.
        cardPurchase:
          description: >
            The card and invoice of a card purchase; null otherwise. A card
            purchase counts in the metrics of its invoice month (DR-035).
          oneOf:
            - $ref: '#/components/schemas/CardPurchase'
            - type: 'null'
        tags:
          type: array
          description: Tags of the transaction, by name.
          items:
            $ref: '#/components/schemas/CategoryReference'
        installment:
          description: Installment number and count of an installment purchase; null otherwise.
          oneOf:
            - $ref: '#/components/schemas/Installment'
            - type: 'null'
    Change:
      type: object
      additionalProperties: false
      required:
        - difference
        - percentChangeTenths
      properties:
        difference:
          type: integer
          description: current - base, in minor units.
        percentChangeTenths:
          type:
            - integer
            - 'null'
          description: >
            Percentage change in tenths of a percent (123 = +12.3%), rounded
            half away from zero; null when the base is zero.
    MetricSet:
      type: object
      additionalProperties: false
      required:
        - realizedIncome
        - realizedExpenses
        - realizedNet
        - forecastIncome
        - forecastExpenses
      properties:
        realizedIncome:
          type: integer
        realizedExpenses:
          type: integer
        realizedNet:
          type: integer
        forecastIncome:
          type: integer
        forecastExpenses:
          type: integer
    MetricChanges:
      type: object
      additionalProperties: false
      required:
        - realizedIncome
        - realizedExpenses
        - realizedNet
        - forecastIncome
        - forecastExpenses
      properties:
        realizedIncome:
          $ref: '#/components/schemas/Change'
        realizedExpenses:
          $ref: '#/components/schemas/Change'
        realizedNet:
          $ref: '#/components/schemas/Change'
        forecastIncome:
          $ref: '#/components/schemas/Change'
        forecastExpenses:
          $ref: '#/components/schemas/Change'
    ComparisonPeriod:
      type: object
      additionalProperties: false
      required:
        - month
        - values
        - changes
      properties:
        month:
          type: string
        values:
          $ref: '#/components/schemas/MetricSet'
        changes:
          $ref: '#/components/schemas/MetricChanges'
    Comparison:
      type: object
      additionalProperties: false
      required:
        - month
        - current
        - previousMonth
        - previousYear
      properties:
        month:
          type: string
        current:
          $ref: '#/components/schemas/MetricSet'
        previousMonth:
          $ref: '#/components/schemas/ComparisonPeriod'
        previousYear:
          $ref: '#/components/schemas/ComparisonPeriod'
    Evolution:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - month
              - realizedIncome
              - realizedExpenses
              - realizedNet
              - observedBalance
            properties:
              month:
                type: string
              realizedIncome:
                type: integer
              realizedExpenses:
                type: integer
              realizedNet:
                type: integer
              observedBalance:
                oneOf:
                  - type: object
                    additionalProperties: false
                    required:
                      - amountMinor
                      - observedOn
                    properties:
                      amountMinor:
                        type: integer
                      observedOn:
                        $ref: '#/components/schemas/FinancialDate'
                  - type: 'null'
    Breakdown:
      type: object
      additionalProperties: false
      required:
        - fromMonth
        - throughMonth
        - previousFromMonth
        - totalMinor
        - previousTotalMinor
        - categories
        - tags
      properties:
        fromMonth:
          type: string
        throughMonth:
          type: string
        previousFromMonth:
          type: string
        totalMinor:
          type: integer
        previousTotalMinor:
          type: integer
        categories:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - name
              - amountMinor
              - shareTenths
              - previousAmountMinor
              - subcategories
            properties:
              id:
                type: string
                format: uuid
              name:
                type: string
              amountMinor:
                type: integer
              shareTenths:
                type:
                  - integer
                  - 'null'
                description: Share of totalMinor in tenths of a percent; null when the total is zero.
              previousAmountMinor:
                type: integer
              subcategories:
                type: array
                items:
                  type: object
                  additionalProperties: false
                  required:
                    - id
                    - name
                    - amountMinor
                  properties:
                    id:
                      type:
                        - string
                        - 'null'
                      format: uuid
                    name:
                      type:
                        - string
                        - 'null'
                      description: Null for expenses without a subcategory.
                    amountMinor:
                      type: integer
        tags:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - id
              - name
              - amountMinor
              - shareTenths
              - previousAmountMinor
            properties:
              id:
                type: string
                format: uuid
              name:
                type: string
              amountMinor:
                type: integer
              shareTenths:
                type:
                  - integer
                  - 'null'
                description: Share of totalMinor in tenths of a percent; null when the total is zero.
              previousAmountMinor:
                type: integer
    ExperienceProfile:
      type: string
      enum:
        - basic
        - intermediate
        - advanced
    SectionOverrides:
      type: object
      additionalProperties: false
      properties:
        observedBalance:
          type: boolean
        realized:
          type: boolean
        forecast:
          type: boolean
        projection:
          type: boolean
        projectionSeries:
          type: boolean
        commitments:
          type: boolean
        analytics:
          type: boolean
    DashboardSections:
      type: object
      additionalProperties: false
      required:
        - observedBalance
        - realized
        - forecast
        - projection
        - projectionSeries
        - commitments
        - analytics
      properties:
        observedBalance:
          type: boolean
        realized:
          type: boolean
        forecast:
          type: boolean
        projection:
          type: boolean
        projectionSeries:
          type: boolean
        commitments:
          type: boolean
        analytics:
          type: boolean
    DashboardPreferences:
      type: object
      additionalProperties: false
      required:
        - profile
        - overrides
        - sections
        - isDefault
      properties:
        profile:
          $ref: '#/components/schemas/ExperienceProfile'
        overrides:
          $ref: '#/components/schemas/SectionOverrides'
        sections:
          $ref: '#/components/schemas/DashboardSections'
        isDefault:
          type: boolean
    Tag:
      type: object
      additionalProperties: false
      required:
        - id
        - name
        - archived
        - version
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        archived:
          type: boolean
        version:
          type: integer
          minimum: 1
    TagList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Tag'
    Installment:
      type: object
      additionalProperties: false
      required:
        - purchaseId
        - number
        - count
      properties:
        purchaseId:
          type: string
          format: uuid
        number:
          type: integer
          minimum: 1
        count:
          type: integer
          minimum: 2
    CardPurchase:
      type: object
      additionalProperties: false
      required:
        - cardId
        - cardName
        - invoiceMonth
        - invoiceSettled
      properties:
        cardId:
          type: string
          format: uuid
        cardName:
          type: string
        invoiceMonth:
          type: string
        invoiceSettled:
          type: boolean
          description: True when the invoice is fully paid; the purchase then counts as realized.
    CardInvoice:
      type: object
      additionalProperties: false
      description: >
        An invoice of a card for a reference month (the month of its due
        date). Before any purchase or date change it does not exist yet:
        version is null and the dates are the card defaults.
      required:
        - cardId
        - referenceMonth
        - closingDate
        - dueDate
        - totalMinor
        - paidMinor
        - outstandingMinor
        - state
        - payments
        - version
        - purchases
        - hasMore
      properties:
        cardId:
          type: string
          format: uuid
        referenceMonth:
          type: string
        closingDate:
          $ref: '#/components/schemas/FinancialDate'
        dueDate:
          $ref: '#/components/schemas/FinancialDate'
        totalMinor:
          type: integer
          minimum: 0
          description: Sum of the non-deleted purchases of the invoice.
        paidMinor:
          type: integer
          minimum: 0
          description: Sum of the payments of the invoice (never expenses, DR-036).
        outstandingMinor:
          type: integer
          description: totalMinor - paidMinor; negative when purchases were removed after payment.
        state:
          type: string
          enum:
            - empty
            - open
            - partially_paid
            - paid
        payments:
          type: array
          items:
            $ref: '#/components/schemas/InvoicePayment'
        version:
          type:
            - integer
            - 'null'
          minimum: 1
        purchases:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        hasMore:
          type: boolean
    CardLimits:
      type: object
      additionalProperties: false
      required:
        - 'on'
        - items
      properties:
        'on':
          $ref: '#/components/schemas/FinancialDate'
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - cardId
              - currentLimitMinor
              - usedMinor
              - availableMinor
            properties:
              cardId:
                type: string
                format: uuid
              currentLimitMinor:
                type:
                  - integer
                  - 'null'
              usedMinor:
                type: integer
              availableMinor:
                type:
                  - integer
                  - 'null'
    CardInvoiceSummaryList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - referenceMonth
              - closingDate
              - dueDate
              - totalMinor
              - paidMinor
              - outstandingMinor
              - state
            properties:
              referenceMonth:
                type: string
              closingDate:
                $ref: '#/components/schemas/FinancialDate'
              dueDate:
                $ref: '#/components/schemas/FinancialDate'
              totalMinor:
                type: integer
              paidMinor:
                type: integer
              outstandingMinor:
                type: integer
              state:
                type: string
                enum:
                  - empty
                  - open
                  - partially_paid
                  - paid
    InvoicePayment:
      type: object
      additionalProperties: false
      required:
        - id
        - amountMinor
        - paidOn
        - recordedAt
      properties:
        id:
          type: string
          format: uuid
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        paidOn:
          $ref: '#/components/schemas/FinancialDate'
        recordedAt:
          type: string
          format: date-time
    InvoiceDatesRequest:
      type: object
      additionalProperties: false
      required:
        - version
        - closingDate
        - dueDate
      properties:
        version:
          description: The invoice version, or null when it does not exist yet.
          type:
            - integer
            - 'null'
          minimum: 1
        closingDate:
          $ref: '#/components/schemas/FinancialDate'
        dueDate:
          $ref: '#/components/schemas/FinancialDate'
    CommitmentInvoice:
      type: object
      additionalProperties: false
      required:
        - cardId
        - cardName
        - referenceMonth
        - dueDate
        - amountMinor
      properties:
        cardId:
          type: string
          format: uuid
        cardName:
          type: string
        referenceMonth:
          type: string
        dueDate:
          $ref: '#/components/schemas/FinancialDate'
        amountMinor:
          type: integer
          description: Open amount of the invoice.
    VersionRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
        sync:
          $ref: '#/components/schemas/SyncContext'
    SyncResolution:
      type: string
      description: >
        auto_merged (independent fields merged), chose_fields (the user
        chose per field), restored (restored to apply an offline edit), or
        deleted_anyway (deleted after a concurrent edit). DR-089, DR-090.
      enum:
        - auto_merged
        - chose_fields
        - restored
        - deleted_anyway
    SyncContext:
      type: object
      additionalProperties: false
      description: >
        Sent when a write resolves an offline synchronization conflict; it
        is recorded in the audit event (ADR-0016).
      required:
        - resolution
        - baseVersion
      properties:
        resolution:
          $ref: '#/components/schemas/SyncResolution'
        baseVersion:
          type: integer
          minimum: 1
          description: The version the offline change was based on.
    UpdateTransactionRequest:
      type: object
      additionalProperties: false
      required:
        - version
      properties:
        version:
          type: integer
          minimum: 1
        sync:
          $ref: '#/components/schemas/SyncContext'
          description: The version the client edited.
        type:
          $ref: '#/components/schemas/TransactionType'
        status:
          $ref: '#/components/schemas/TransactionStatus'
        description:
          type: string
          minLength: 1
          maxLength: 140
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        financialDate:
          $ref: '#/components/schemas/FinancialDate'
        categoryId:
          type: string
          format: uuid
        subcategoryId:
          type:
            - string
            - 'null'
          format: uuid
        invoiceMonth:
          type: string
          pattern: '^\d{4}-(0[1-9]|1[0-2])$'
          description: Moves a card purchase to another invoice of the same card.
        tagIds:
          type: array
          maxItems: 10
          uniqueItems: true
          description: Replaces the tags; tags already on the transaction may be archived; tags never change amounts (DR-013).
          items:
            type: string
            format: uuid
    TransactionList:
      type: object
      additionalProperties: false
      required:
        - items
        - hasMore
        - nextCursor
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        hasMore:
          type: boolean
          description: True when more transactions match beyond this page.
        nextCursor:
          type:
            - string
            - 'null'
          description: Pass as cursor to fetch the next page; null on the last page.
    RecordBalanceSnapshotRequest:
      type: object
      additionalProperties: false
      required:
        - amountMinor
        - observedOn
      properties:
        amountMinor:
          type: integer
          minimum: -99999999999
          maximum: 99999999999
          description: Observed balance in minor units; may be zero or negative.
        observedOn:
          $ref: '#/components/schemas/FinancialDate'
        note:
          type:
            - string
            - 'null'
          maxLength: 140
    BalanceSnapshot:
      type: object
      additionalProperties: false
      required:
        - id
        - amountMinor
        - currency
        - observedOn
        - note
        - recordedAt
      properties:
        id:
          type: string
          format: uuid
        amountMinor:
          type: integer
          minimum: -99999999999
          maximum: 99999999999
        currency:
          type: string
          pattern: '^[A-Z]{3}$'
        observedOn:
          $ref: '#/components/schemas/FinancialDate'
        note:
          type:
            - string
            - 'null'
        recordedAt:
          type: string
          format: date-time
    BalanceSnapshotList:
      type: object
      additionalProperties: false
      required:
        - items
        - current
        - hasMore
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BalanceSnapshot'
        current:
          oneOf:
            - $ref: '#/components/schemas/BalanceSnapshot'
            - type: 'null'
        hasMore:
          type: boolean
    BalanceReminderFrequency:
      type: string
      enum:
        - app_start
        - daily
        - every_n_days
        - never
    BalanceReminderSetting:
      type: object
      additionalProperties: false
      required:
        - frequency
        - intervalDays
      properties:
        frequency:
          $ref: '#/components/schemas/BalanceReminderFrequency'
        intervalDays:
          type:
            - integer
            - 'null'
          minimum: 1
          maximum: 90
          description: Required (1 to 90) for every_n_days; null otherwise.
    BalanceReminder:
      type: object
      additionalProperties: false
      required:
        - frequency
        - intervalDays
        - isDefault
      properties:
        frequency:
          $ref: '#/components/schemas/BalanceReminderFrequency'
        intervalDays:
          type:
            - integer
            - 'null'
        isDefault:
          type: boolean
    MonthlyDashboard:
      type: object
      additionalProperties: false
      required:
        - month
        - currency
        - realizedIncome
        - realizedExpenses
        - realizedNet
        - forecastIncome
        - forecastExpenses
        - realizedExpensesByCategory
        - observedBalance
        - projection
      properties:
        month:
          type: string
        currency:
          type: string
          pattern: '^[A-Z]{3}$'
        realizedIncome:
          type: integer
          description: M-001, minor units.
        realizedExpenses:
          type: integer
          description: M-002, minor units.
        realizedNet:
          type: integer
          description: M-003 = M-001 - M-002; may be negative.
        forecastIncome:
          type: integer
          description: M-004, minor units.
        forecastExpenses:
          type: integer
          description: M-005, minor units.
        realizedExpensesByCategory:
          type: array
          description: M-006, sorted by amount (desc), then name.
          items:
            type: object
            additionalProperties: false
            required:
              - categoryId
              - name
              - amountMinor
            properties:
              categoryId:
                type: string
                format: uuid
              name:
                type: string
              amountMinor:
                type: integer
        observedBalance:
          description: M-007, the latest observed balance dated within or before the month.
          oneOf:
            - type: object
              additionalProperties: false
              required:
                - amountMinor
                - observedOn
              properties:
                amountMinor:
                  type: integer
                observedOn:
                  $ref: '#/components/schemas/FinancialDate'
            - type: 'null'
        projection:
          description: M-008 projected month-end balance; null without an observed balance.
          oneOf:
            - $ref: '#/components/schemas/Projection'
            - type: 'null'
    RecurrenceFrequency:
      type: string
      enum:
        - monthly
        - weekly
        - yearly
    NonBusinessDayRule:
      type: string
      description: What to do when an occurrence falls on a non-business day.
      enum:
        - keep
        - previous
        - next
    CreateRecurrenceRequest:
      type: object
      additionalProperties: false
      required:
        - type
        - description
        - amountMinor
        - categoryId
        - frequency
        - nonBusinessDayRule
        - startDate
      properties:
        type:
          $ref: '#/components/schemas/TransactionType'
        description:
          type: string
          minLength: 1
          maxLength: 140
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        categoryId:
          type: string
          format: uuid
        subcategoryId:
          type:
            - string
            - 'null'
          format: uuid
        frequency:
          $ref: '#/components/schemas/RecurrenceFrequency'
        nonBusinessDayRule:
          $ref: '#/components/schemas/NonBusinessDayRule'
        startDate:
          $ref: '#/components/schemas/FinancialDate'
        endDate:
          type:
            - string
            - 'null'
          format: date
    RecurrenceSeries:
      type: object
      additionalProperties: false
      required:
        - id
        - type
        - description
        - amountMinor
        - currency
        - categoryId
        - subcategoryId
        - frequency
        - nonBusinessDayRule
        - startDate
        - endDate
        - materializedThrough
        - version
      properties:
        id:
          type: string
          format: uuid
        type:
          $ref: '#/components/schemas/TransactionType'
        description:
          type: string
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        currency:
          type: string
          pattern: '^[A-Z]{3}$'
        categoryId:
          type: string
          format: uuid
        subcategoryId:
          type:
            - string
            - 'null'
          format: uuid
        frequency:
          $ref: '#/components/schemas/RecurrenceFrequency'
        nonBusinessDayRule:
          $ref: '#/components/schemas/NonBusinessDayRule'
        startDate:
          $ref: '#/components/schemas/FinancialDate'
        endDate:
          type:
            - string
            - 'null'
          format: date
        materializedThrough:
          type:
            - string
            - 'null'
          format: date
        version:
          type: integer
          minimum: 1
    RecurrenceSeriesCreated:
      type: object
      additionalProperties: false
      required:
        - series
        - occurrencesCreated
      properties:
        series:
          $ref: '#/components/schemas/RecurrenceSeries'
        occurrencesCreated:
          type: integer
          minimum: 0
    UpdateRecurrenceRequest:
      type: object
      additionalProperties: false
      required:
        - version
        - fromOccurrenceDate
      properties:
        version:
          type: integer
          minimum: 1
        fromOccurrenceDate:
          $ref: '#/components/schemas/FinancialDate'
        description:
          type: string
          minLength: 1
          maxLength: 140
        amountMinor:
          $ref: '#/components/schemas/AmountMinor'
        categoryId:
          type: string
          format: uuid
        subcategoryId:
          type:
            - string
            - 'null'
          format: uuid
    RecurrenceSeriesChanged:
      type: object
      additionalProperties: false
      required:
        - series
        - occurrencesAffected
      properties:
        series:
          $ref: '#/components/schemas/RecurrenceSeries'
        occurrencesAffected:
          type: integer
          minimum: 0
    RecurrenceSeriesList:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/RecurrenceSeries'
    FlowTotals:
      type: object
      additionalProperties: false
      required:
        - income
        - expenses
      properties:
        income:
          type: integer
        expenses:
          type: integer
    Projection:
      type: object
      additionalProperties: false
      description: >
        M-008. amountMinor = base.amountMinor + (afterObservation.income -
        afterObservation.expenses) + (pendingUpToObservation.income -
        pendingUpToObservation.expenses) - openInvoices - invoicePayments.
        Card purchases enter only through their invoices. A calculation, never an observed
        balance.
      required:
        - amountMinor
        - base
        - afterObservation
        - pendingUpToObservation
        - openInvoices
        - invoicePayments
      properties:
        amountMinor:
          type: integer
        base:
          type: object
          additionalProperties: false
          required:
            - amountMinor
            - observedOn
          properties:
            amountMinor:
              type: integer
            observedOn:
              $ref: '#/components/schemas/FinancialDate'
        afterObservation:
          $ref: '#/components/schemas/FlowTotals'
        pendingUpToObservation:
          $ref: '#/components/schemas/FlowTotals'
        openInvoices:
          type: integer
          description: >
            Amount of card invoices due before the end of the month not
            covered by payments dated before the end of the month.
        invoicePayments:
          type: integer
          description: Invoice payments dated after the observation and before the end of the month.
    ProjectionSeries:
      type: object
      additionalProperties: false
      required:
        - items
      properties:
        items:
          type: array
          items:
            type: object
            additionalProperties: false
            required:
              - month
              - projectedBalance
            properties:
              month:
                type: string
              projectedBalance:
                type:
                  - integer
                  - 'null'
    CommitmentSection:
      type: object
      additionalProperties: false
      required:
        - items
        - invoices
        - hasMore
        - income
        - expenses
      properties:
        items:
          type: array
          description: Pending transactions other than card purchases.
          items:
            $ref: '#/components/schemas/Transaction'
        invoices:
          type: array
          description: Open card invoices due in the section, by due date.
          items:
            $ref: '#/components/schemas/CommitmentInvoice'
        hasMore:
          type: boolean
        income:
          type: integer
          description: Sum of pending income in the section, minor units.
        expenses:
          type: integer
          description: Sum of pending expenses and open invoices in the section, minor units.
    Commitments:
      type: object
      additionalProperties: false
      required:
        - from
        - through
        - overdue
        - upcoming
      properties:
        from:
          $ref: '#/components/schemas/FinancialDate'
        through:
          $ref: '#/components/schemas/FinancialDate'
        overdue:
          $ref: '#/components/schemas/CommitmentSection'
        upcoming:
          $ref: '#/components/schemas/CommitmentSection'
    ErrorResponse:
      type: object
      additionalProperties: false
      required:
        - error
      properties:
        error:
          type: object
          additionalProperties: false
          required:
            - code
            - message
          properties:
            code:
              type: string
              description: Stable machine-readable error code.
            message:
              type: string
              description: Human-readable description; not localized.
